---
title: httpz
---

import Link from 'next/link';
import { Button } from "@components/ui/button"
import {ChevronRight} from "lucide-react"
import {featureListCN, Card} from "@components/data/data"
import { div, i } from 'framer-motion/client';

export default function IndexPage() {
  return (
    <div className="relative isolate min-h-screen w-full">
      <div className="mx-auto py-5">
        <div className="text-center flex flex-col gap-4">
          <h2 className="text-8xl font-bold tracking-tight text-[#4cf9f7] sm:text-6xl ">
           HTTPZ 
          </h2>
          <p className="font-serif text-xl">轻量级的 net/http 1.22+ 增强库，融合了 <span className='font-semibold'>chi & Echo </span> 的优点，
           <br></br> 让你能够轻松地使用 net/http 编写处理器</p>

             <img src='./logo/httpz.png' alt='httpz.png' className='rounded-lg mx-auto sm:w-5/6 lg:w-1/2 w-full' />
          <div className='mt-10'>
          <Link href={"/cn/docs"}>
          <Button variant="outline" className="h-12 font-semibold text-white text-xl  px-10 bg-[#2563eb] hover:scale-110 transition-transform"> 
            Get Started
          </Button>
          </Link>
          </div>

          <div className='mt-20'>
            <h2 className='font-semibold text-4xl mb-4'> Features</h2>
            <p className='font-sm text-slate-700 mb-4'>轻松使用 net/http 构建现代应用</p>
          </div>
          <div className='flex flex-wrap gap-10 justify-center'>
            {
              featureListCN.map((item, index) => {
                return (
                  <div className='flex flex-col gap-4 rounded-lg items-start justify-center border-2 lg:w-1/4  sm:w-1/3 px-5 py-4'>
                    <div className='flex gap-2 font-bold'>
                    <div> {item.icon} </div>
                    <div> {item.title} </div>
                    </div>
                    <div className='text-slate-500'> {item.description}</div>
                  </div>
                )
              })
            }
          </div>
          
          <div className='mt-10 flex flex-col gap-10'>
            <h1 className='text-4xl'>基准测试</h1>
            <img src='./bench/benchmark.png' alt='httpz.png' className='rounded-lg mx-auto sm:w-5/6 lg:w-1/2 w-full' />
          </div>
        </div>
      </div>
    </div>
  );
}

<IndexPage />
